#include <stdio.h>
#include<string.h>

int main(){
    char str[20];
    int length;

    printf("munroe\n");
    length=strlen(str);
    printf("The length of the string is:%d\n",length);
    printf("%19s\n",str);
    scanf("%s\n",str);
return 0;
}
